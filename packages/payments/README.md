# @oobe-protocol-labs/sap-mcp-payments

Internal package boundary for `packages/payments/src`.

This package owns the x402/pay.sh payment gate, pricing catalog, usage ledger,
prepaid credit store, MCP session cache, hosted tool eligibility checks,
JSON-RPC parsing helpers, schema validation, and the self-hosted OOBE x402 SVM
facilitator server.