# @oobe-protocol-labs/sap-mcp-ui-cards

Internal package boundary for `src/ui`.

This package owns MCP Apps Card rendering, protocol logos, shell templates, and
`ui://` resource construction. It must remain rendering-only.
