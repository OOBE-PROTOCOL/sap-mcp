# @oobe-protocol-labs/sap-mcp-schemas

Internal package boundary for `packages/schemas/src`.

This package owns public schema contracts, Zod/JSON schema exports, MCP
resource shapes, and stable public protocol data models.
