# @oobe-protocol-labs/sap-mcp-core

Internal package boundary for `src/core`.

This package owns shared contracts, constants, logger/error/result primitives,
and types that must not depend on higher-level runtime modules.
